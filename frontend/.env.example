# Example env for frontend
# Set this to the backend base URL in production, e.g. https://api.example.com
# In development you can leave this unset and the dev proxy in next.config.mjs will forward /api/backend -> http://localhost:8000
NEXT_PUBLIC_API_BASE=
